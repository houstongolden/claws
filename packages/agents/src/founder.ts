import type { AgentDefinition } from "@claws/shared/types";

export const founderAgent: AgentDefinition = {
  id: "founder",
  description: "Lead agent for strategy, roadmap, and product decisions",
  modes: ["founder"]
};
