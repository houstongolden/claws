{
  "name": "@claws/runtime-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo lint runtime-db",
    "test": "echo test runtime-db",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@claws/shared": "workspace:*",
    "@electric-sql/pglite": "^0.3.15"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
